name: Build, Test & Release PRX

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to create (e.g. v1.0)'
        required: true
        default: 'v1.0'
  push:
    branches: [main, master, feat/*]
    tags: ['v*']
  pull_request:
    branches: [main, master]

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Host unit tests (classifier + SFO parser)
        run: make test

  build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Build PRX via Docker
        run: |
          docker run --rm -v "${{ github.workspace }}:/src" -w "/src" pspdev/pspdev make release

      - name: Verify build artifacts exist
        run: |
          test -f autocat.prx || (echo "FAIL: autocat.prx not produced" && exit 1)
          test -f favtool/EBOOT.PBP || (echo "FAIL: favtool/EBOOT.PBP not produced" && exit 1)
          ls -la autocat.prx favtool/EBOOT.PBP
          echo "prx_size=$(stat -c%s autocat.prx)" >> "$GITHUB_OUTPUT"
        id: verify

      - name: Package release (zip on host — pspdev image has no zip)
        run: |
          command -v zip >/dev/null 2>&1 || (sudo apt-get update -qq && sudo apt-get install -y -qq zip)
          make pack

      - name: Collect artifacts
        run: |
          mkdir -p artifacts
          cp autocat.prx artifacts/
          cp favtool/EBOOT.PBP artifacts/favtool-EBOOT.PBP
          find build -name "*.zip" -exec cp {} artifacts/ \;
          ls -la artifacts/

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: autocat-build
          path: artifacts/

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}
          files: artifacts/*
          generate_release_notes: true